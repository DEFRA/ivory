'use strict'

import blippPkg from 'blipp/lib/index.js'
// Blipp plugin is used to display the routes table to console at startup
export const plugin = {
  plugin: blippPkg,
  options: {}
}

export default plugin // ES6 default export
